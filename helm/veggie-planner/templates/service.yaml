apiVersion: v1
kind: Service
metadata:
  name: {{ include "veggie-planner.fullname" . }}
  labels:
    {{- include "veggie-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "veggie-planner.selectorLabels" . | nindent 4 }}
